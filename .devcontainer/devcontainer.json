{
  "name": "electron-playground",
  "dockerComposeFile": ["docker-compose.yml"],
  "service": "workspace",
  "workspaceFolder": "/workspace",
  "customizations": {
    "vscode": {
      "extensions": [
        "EditorConfig.EditorConfig"
      ]
    }
  },
  "features": {
  }
  // "postStartCommand": "sudo /etc/init.d/dbus start"
}
